<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Video">
	<insert id="insertTime">
		insert into videocheck (videoID, studentID ,lastTime, timer, watch)
		values
		(#{videoID}, #{studentID}, #{lastTime}, #{timer}, #{watch})
		
	</insert>
	
	<update id="updateTime">
		update videocheck
		set lastTime=#{lastTime}, timer=#{timer}
		where studentID=#{studentID} AND videoID=#{videoID}
	</update>
	
	<update id="updateWatch">
		update videocheck
		set lastTime=#{lastTime}, timer=#{timer}, watch=#{watch}
		where studentID=#{studentID} AND videoID=#{videoID}
	</update>
	
	<delete id="deleteTime">
		delete from videocheck where id=#{id}
	</delete>
	
	<select id="getTime" resultType="VideoVO">
		select * 
		from videocheck 
		where id=#{id}
	</select>
	
	<select id="getTimeList" resultType="VideoVO">
		SELECT * 
		FROM videocheck vc
		LEFT OUTER JOIN Video v ON vc.videoID = v.id
		where studentID=#{studentID} AND videoID=#{videoID}
	</select>
	
</mapper>