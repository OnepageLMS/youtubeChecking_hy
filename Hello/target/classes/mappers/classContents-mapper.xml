<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ClassContents">
	<insert id="insertContent">
		insert into lms_classPlaylist (title, description, classID, playlistID, week, day, daySeq, startDate, endDate)
		values (#{title}, #{description}, #{classID}, #{playlistID}, #{week}, #{day}, 
			#{daySeq}, #{startDate}, #{endDate})
	</insert>
	
	<select id="getDaySeq" resultType="int">
		select count(*) from lms_classPlaylist 
		where classID=#{classID} and week=#{week} and day=#{day}
	</select>
	
	<update id="updateContent">
		update lms_classPlaylist
		set title=#{title}, description=#{description}, startDate=#{startDate}, endDate=#{endDate}, playlistID=#{playlistID} 
		where id=#{id}
	</update>
	
	<delete id="deleteContent">
		delete from lms_classPlaylist where id=#{id}
	</delete>
	
	<select id="getOneContent" resultType="ClassContentsVO">
		select * from lms_classPlaylist
		where id=#{id}
	</select>
	
	<select id="getWeekClassContents" resultType="ClassContentsVO">
		select * 
		from lms_classPlaylist join Video on lms_classPlaylist.playlistID = Video.playlistID
		where classID=#{classID} AND published =1 order by week, day, daySeq, seq

	</select>
	
	<select id="getSamePlaylistID" resultType="ClassContentsVO">
		SELECT * 
		FROM lms_classPlaylist
		where playlistID = #{playlistID} 
	</select>
	
	<select id="getAllClassContents" resultType="ClassContentsVO">
		select * from lms_classPlaylist
		where classID=#{classID} order by week and day and daySeq
	</select>

</mapper>